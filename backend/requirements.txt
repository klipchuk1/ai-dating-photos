# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# HTTP / async
httpx==0.27.0
requests==2.32.3
aiofiles==23.2.1

# Replicate
replicate==0.25.1

# Face identity verification (primary — ArcFace embeddings)
insightface==0.7.3
onnxruntime==1.18.0          # CPU inference
# onnxruntime-gpu==1.18.0    # swap for GPU

# Face similarity fallback (if insightface unavailable)
deepface==0.0.93
# tensorflow-cpu==2.16.1     # lighter TF build for deepface

# Computer vision (face selection + histogram fallback)
opencv-python-headless==4.9.0.80
numpy==1.26.4

# Config
python-dotenv==1.0.1
pydantic==2.7.1
